
N_SCANNER = spawn(container, idling, [actor_scanner:create()]).
WS= spawn(container, idling, [actor_workstation:create()]).
R = spawn(container, idling, [actor_railway:create()]).

WS! R! N_SCANNER ! {start}.

R ! {self(), {add, option, {scanner, N_SCANNER}}}.
WS ! {self(), {add, out, R}}.


P1 = actor_product:create().
WS ! {self(), {actor_product, P1}}.