
N_SCANNER = spawn(container, idling, [actor_scanner:create()]).
R2 = spawn(container, idling, [actor_railway:create()]).
R2 ! {self(), {add, option, {scanner, N_SCANNER}}}.

R2! N_SCANNER ! {start}.

P1 = actor_product:create().
R2 ! {self(), {actor_product, P1}}.